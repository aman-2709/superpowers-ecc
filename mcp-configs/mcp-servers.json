{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "MCP server configuration templates for superpowers-ecc. Copy entries to your ~/.claude/settings.json under mcpServers.",
  "servers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "<your-token>"
      },
      "description": "GitHub operations — PRs, issues, repos, code search"
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@latest"],
      "description": "Live documentation lookup for any library"
    },
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"],
      "description": "Chain-of-thought reasoning for complex problems"
    }
  }
}
